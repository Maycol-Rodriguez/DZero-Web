import { ListReports } from '../components';

export const Reportes = () => {
  return (
    <div className='flex flex-wrap gap-5 justify-center transition-all'>
      <ListReports />
    </div>
  );
};
